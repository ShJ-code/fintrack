spring.application.name=fintrack-backend
server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/fintrack_db}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:password}

# CORS ? use one source of truth across all controllers
app.cors.origin=${FRONTEND_ORIGIN:http://localhost:5173}